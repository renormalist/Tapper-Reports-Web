---

name: Artemis::Reports::Web

database:
  ReportsDB:
    dsn:        dbi:mysql:dbname=reportsdb
    username:   artemis
    password:   artemis


# explicit development environment
development:
  database:
    ReportsDB:
      dsn:        dbi:mysql:dbname=reportsdb_dev
      username:   artemis
      password:   artemis


# special test environment
test:
  database:
    ReportsDB:
      dsn:        dbi:SQLite:dbname=t/artemis_schema_reportsdb_test.sqlite
      username:   ''
      password:   ''

# live environment, mini hash structure must exist
live:
  test_value:     live
